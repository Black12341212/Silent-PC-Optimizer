# 🚀 Silent PC Optimizer (Тихий Оптимизатор)

Легковесный Python-скрипт, который работает в фоне и автоматически оптимизирует работу ПК, когда система начинает тормозить.

## ⚡ Что он делает?
Скрипт висит в фоне и каждые 30 секунд проверяет загрузку оперативной памяти (RAM). 
Если загрузка превышает **85%**, "Тихий оптимизатор" вступает в дело:

1.  🧹 **Чистит системный кэш** и временные файлы (`%TEMP%`).
2.  📉 **Сворачивает неактивные окна браузеров** (Chrome, Firefox, Opera и др.), чтобы снизить нагрузку на рендеринг и GPU.
3.  🔔 **Показывает отчет** о проделанной работе через нативное уведомление Windows.

> **Пример отчета:** *"Освободил 1.2 ГБ RAM, очищено 450 МБ кэша, свернуто 3 окна."*

## 🛠 Технологии
Написан на **Python**. Использует библиотеки:
*   `psutil` — для мониторинга системных ресурсов.
*   `pygetwindow` — для управления окнами приложений.
*   `ctypes` — для нативных уведомлений Windows.

## 📦 Установка и запуск

1.  **Скачайте проект:**
    ```bash
    git clone https://github.com/Black12341212/Silent-PC-Optimizer.git
    ```
2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
3.  **Запустите скрипт:**
    Дважды кликните по файлу `optimizer.pyw`. 
    
    *Примечание: Расширение `.pyw` запускает скрипт без окна консоли (полностью скрытно).*

## ⚙️ Настройка
Вы можете открыть файл `optimizer.pyw` в любом текстовом редакторе и изменить параметры под себя:

```python
RAM_THRESHOLD = 85.0  # При каком % занятой памяти срабатывать

CHECK_INTERVAL = 30   # Как часто проверять систему (в секундах)

"Также посмотрите мой проект SmartDownloadsSorter — умная сортировка файлов с удобным меню." (https://github.com/Black12341212/SmartDownloadsSorter).
И наоборот. Те, кто ищет сортировщик, часто интересуются и оптимизацией ПК.

